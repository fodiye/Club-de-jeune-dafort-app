# ========================================
# Jeunes en Action de Dafort - Production
# ========================================

# Port d'exposition (default: 80)
PORT=80

# Strapi secrets (OBLIGATOIRE - générer des valeurs uniques)
# Générer avec: openssl rand -base64 16
APP_KEYS=key1,key2,key3,key4
API_TOKEN_SALT=change-me-api-token-salt
ADMIN_JWT_SECRET=change-me-admin-jwt-secret
TRANSFER_TOKEN_SALT=change-me-transfer-token-salt
JWT_SECRET=change-me-jwt-secret
ENCRYPTION_KEY=change-me-encryption-key

# URL du frontend (pour le preview mode Strapi)
CLIENT_URL=http://localhost

# SMTP pour les notifications email (optionnel)
SMTP_HOST=
SMTP_PORT=587
SMTP_LOGIN=
SMTP_KEY=
MAIL_FROM_NAME=Jeunes en Action de Dafort
MAIL_FROM_EMAIL=
MAIL_TO_EMAIL=
